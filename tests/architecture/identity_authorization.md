# Identity and Authorization Test Targets

- Authentication alone must not grant resource access.
- Unknown actors must fail closed.
- Explicitly granted rights must be enforceable.
- Revoked rights must fail on subsequent authorization checks.
- Agent identity must be distinguishable from the human or organization authorizing it.
