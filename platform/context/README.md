# Context Runtime

Assembles the minimum authorized context for a user task.

Context is persistent across devices and sessions, but rights are evaluated for every access.

The runtime must not copy or expose unrelated private context merely to provide continuity.