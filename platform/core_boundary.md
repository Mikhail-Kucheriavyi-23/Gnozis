# Core Boundary v1

The public platform never becomes the authority for trusted state evolution.

Core is a separate trusted runtime. Platform communicates with it through explicit contracts and receives only authorized results.

Core must not depend on UI, GitHub, payment systems, LLM providers, or domain-specific research repositories.