# Gnozis Audit — 2026-09-10 to 2026-09-12

## Purpose

Record the transition from adversarial validation to Core reconciliation and preserve the engineering work performed during the period.

## 2026-09-10 — audit consolidation

- Reconstructed the current project state and historical engineering findings.
- Established Living Context / audit hierarchy.
- Preserved the rule that repository evidence outranks conversational claims.
- Identified the main Core discrepancies: identity default, relation persistence, loose Test contract, bool-as-int step count, shallow immutability, duplicate/isolated Ψ boundary.
- Kept protected internal memory as a historical recovery GAP rather than inventing a replacement.

## 2026-09-11 — adversarial validation

The project expanded regression evidence around deterministic trajectories, external-context independence, state sufficiency, selector invariance/independence, X/R sensitivity, relation representation, relation extinction/regeneration, sequential evolution, locality, causal closure, and memory provenance.

These tests strengthen evidence for declared contracts, but do not by themselves prove that every live Core path uses the same canonical Ψ transition.

## 2026-09-12 — Core reconciliation

### Code changes

- `core/engine.py`: strict `type(steps) is int`; canonical `PsiTransition` execution support.
- `core/evolution.py`: strict `Test(candidate) -> bool`; added `evolutionary_psi_transition()` as explicit `Psi -> Psi` evolution with State as implementation adapter.
- `core/uroboros.py`: removed silent identity evolution; `Uroboros.evolutionary()` routes through `PsiTransition`.
- `core/psi_transition.py`: corrected the State adapter to use `State.to_psi()` / `State.from_psi()`.
- Relation boundary: canonical relation representation is an immutable tuple of `Relation` objects.

### Tests and evidence

- Existing repaired-contract and relation regression coverage retained.
- Integration coverage verifies evolutionary `Uroboros` and direct Engine execution through `PsiTransition`.
- Added explicit legacy transition boundary tests.
- Added usage-classification evidence separating generic Engine behavior from canonical Ψ evidence.

### Documentation added

- `docs/LEGACY_TRANSITION_BOUNDARY.md` — temporary State-callable compatibility boundary.
- `docs/ENGINE_USAGE_CLASSIFICATION.md` — classification of Engine callers/evidence.
- `docs/CORE_CONFORMANCE_MATRIX.md` remains the canonical contract/status matrix.
- `docs/OPEN_DEFECTS.md` remains the active engineering defect ledger.

## Reconciliation result

The canonical Ψ boundary is executable through the live Engine/Uroboros evolutionary path. The State adapter uses the actual `to_psi()` / `from_psi()` contract.

The compatibility State-callable path remains intentionally available for existing code. Plain `State -> State` tests establish generic Engine/compatibility behavior, not canonical Ψ semantics. The compatibility path is therefore bounded rather than silently conflated with Ψ evidence.

Current canonical target:

`Ψ → PsiTransition → State adapter → Engine → Uroboros → Ψ`

## Current stage

**Adversarial Validation → Core Reconciliation (integration + compatibility-boundary passes complete)**

## Next gate

Full regression and adversarial regression against the reconciled Core. Then audit remaining supported callers before deciding whether the compatibility path can be removed.

## Remaining work

- full-suite verification of the reconciled Core;
- full adversarial regression after integration changes;
- final compatibility-removal decision;
- consolidate canonical X/R representation;
- re-audit Internet Port against Core authority/provenance boundary;
- verify actual GitHub Actions evidence for current HEAD;
- recover historical protected-memory specification before implementation;
- continue interoperability and mathematical/physics research only after the Core gates above.

## Evidence discipline

A passing test is evidence for the tested implementation and contract. It is not automatically a mathematical proof of the general theory. Historical claims remain marked as recovered, reconstructed, or GAP according to the recovery ledger.
